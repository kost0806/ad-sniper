{
  "name": "ad-sniper",
  "version": "0.0.2",
  "description": "Block only the ads you don't want to see",
  "scripts": {
    "build": "vite build && vite build --mode content && vite build --mode worker",
    "dev": "npm run build -- --watch"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "vite": "^5.0.0"
  }
}
